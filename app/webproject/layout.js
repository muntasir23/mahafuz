import Link from "next/link";

export const metadata = {
  title: "Projects || Mahafuz Ahamed",
  description: "This is a about page",
};

export default function AboutLayout({ children }) {
  return <div>{children}</div>;
}
